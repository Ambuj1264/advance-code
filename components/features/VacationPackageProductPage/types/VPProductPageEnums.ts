export enum VPStaysTypes {
  BUDGET = "budget",
  COMFORT = "comfort",
  QUALITY = "quality",
}

export enum VPPriceChange {
  STAYED_THE_SAME = "STAYED_THE_SAME",
  WENT_UP = "WENT_UP",
  WENT_DOWN = "WENT_DOWN",
}

export enum VPPriceType {
  FROM = "FROM",
  ESTIMATED = "ESTIMATED",
  REAL = "REAL",
}

export enum VPTourSelectedSpecs {
  STARTS = "Starts",
  TIME = "Time",
  DURATION = "Duration",
  DIFFICULTY = "Difficulty",
}

export enum VPQuickfactId {
  CarRental = "CarRental",
  Flight = "Flight",
  Hotel = "Hotel",
}
