query CategeryHeaderQuery($slug: String) {
  tourCategoryHeader(slug: $slug) {
    id,
    name,
    tours,
    description,
    url,
    imageUrl,
    averageReviewScore,
    reviewCount,
    pageAboutTitle,
    pageAboutDescription
  }
}
