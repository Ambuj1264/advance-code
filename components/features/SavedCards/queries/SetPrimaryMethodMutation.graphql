#import "./SavedCardsFragment.graphql"

mutation setCardAsPrimary($input: PaymentSetCardAsPrimaryParamsInput!) {
  setCardAsPrimary(input: $input) {
    savedCards {
      ...SavedCardsFragment
    }
  }
}