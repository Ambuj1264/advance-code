query flightQuery ($input: CheckFlightParamsInput!) {
  flightCheckFlight(input: $input) {
    sessionId
    bookingToken
    flightsChecked
    flightsInvalid
    priceChange
    totalBagPrice
    flightPrice
    showHealthDeclaration: showHealthDecleration
  }
}