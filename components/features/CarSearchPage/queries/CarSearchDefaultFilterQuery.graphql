query CarSearchDefaultFilterQuery($input: CarGetOffersInput!) {
  carOffers: carGetOffers(input: $input) {
    filters {
      type
      options: items {
        optionId: id
        name
      }
    }
  }
}
