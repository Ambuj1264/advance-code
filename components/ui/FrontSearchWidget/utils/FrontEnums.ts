export enum SearchTabsEnum {
  Trips,
  Stays,
  Cars,
  Flights,
  VacationPackages,
}

export enum SearchTabDataTestIds {
  Trips = "trips-tab-label",
  Stays = "stays-tab-label",
  Cars = "cars-tab-label",
  Flights = "flights-tab-label",
  VacationPackages = "vacation-tab-label",
}

export enum VacationsMobileStepEnum {
  Details,
  Dates,
}

export enum TripsMobileStepsEnum {
  Details,
  Dates,
}

export enum StaysMobileStepsEnum {
  Details,
  Dates,
}

export enum CarsMobileStepsEnum {
  Details,
  Dates,
}

export enum FlightsMobileStepsEnum {
  Details,
  Dates,
}

export enum ActiveLocationAutocomplete {
  None,
  Origin,
  Destination,
}
