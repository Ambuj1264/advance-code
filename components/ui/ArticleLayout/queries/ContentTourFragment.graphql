fragment ContentTourFragment on Tour {
  id
  name
  linkUrl: front_url
  slug: uri
  description: description_excerpt
  averageRating: review_score_formatted
  reviewsCount: review_count
  basePrice: base_price
  discount {
    value
  }
  banner {
    text
  }
  image {
    id
    url: front_url
    name
    alt
  }
  specs {
    iconId: id
    name
    value
  }
  props {
    iconId: id
    title
  }
}
