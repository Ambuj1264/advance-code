import React from "react";

import TeaserList from "components/ui/TeaserList/TeaserList";

const ArticleWidgetListOfTeasers = (props: ArticleWidgetTypes.ArticleWidgetListOfTeasers) => {
  return <TeaserList {...props} />;
};

export default ArticleWidgetListOfTeasers;
